import { Project, Profile, CommentItem } from "../types";

/**
 * Clean data structures - All data is fetched dynamically from Supabase database.
 * No mock or dummy records remain.
 */
export const MOCK_CREATORS: Profile[] = [];

export const MOCK_PROJECTS: Project[] = [];

export const MOCK_COMMENTS: CommentItem[] = [];
